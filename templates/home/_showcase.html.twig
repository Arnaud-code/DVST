<a href="{{ path('category_show', {'slug': item.slug}) }}">
	<div class="container mt-5">
		<h2>{{ item.name }}</h2>
	</div>
	<div class="container-fluid gx-0">
		<img src="/showcase/{{ item.path }}.jpg" alt="img" width="100%">
	</div>
</a>
