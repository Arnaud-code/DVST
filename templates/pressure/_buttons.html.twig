<div class="mt-5" style="display: flex; flex-direction: row; justify-content: space-around">
	<div>
		<a href="{{ path("pressure_list") }}" class="btn btn-primary">
			<i class="fas fa-list"></i>
			Liste
		</a>
	</div>
	<div>
		<a href="{{ path("pressure_edit") }}" class="btn btn-primary">
			<i class="fas fa-plus"></i>
			Nouveau
		</a>
	</div>
	<div>
		<a href="{{ path("pressure_calculation") }}" class="btn btn-primary">
			<i class="fas fa-calculator"></i>
			Calcul
		</a>
	</div>
</div>